#include<stdio.h>

int main()
{
	int i,j;
	
	//loop for the last index upto which we have to continue comparison
	for()
	{
		//loop for i and i+1 to compare a[i] and a[i+1]
		for()
		{
				if(a[i]>a[i+1])
				{
					//exchange a[i] and a[i+1]
					//use temp to exchange
				}
		}
	}
}
